    select case (seletor)
        case (lista de rótulos 1)
            bloco1
        case (lista de rótulos 2)
            bloco2
        ...
        case (lista de rótulos N)
            blocoN
        case default
            bloco_padrao
    end select
